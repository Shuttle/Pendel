import{_ as t,o as a,c as s,ag as o}from"./chunks/framework.BuUvxvtQ.js";const n="/images/endpoint.png",p=JSON.parse('{"title":"The Broad Strokes","description":"","frontmatter":{},"headers":[],"relativePath":"shuttle-hopper/home.md","filePath":"shuttle-hopper/home.md"}'),r={name:"shuttle-hopper/home.md"};function i(h,e,d,m,c,l){return a(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="the-broad-strokes" tabindex="-1">The Broad Strokes <a class="header-anchor" href="#the-broad-strokes" aria-label="Permalink to &quot;The Broad Strokes&quot;">​</a></h1><p>Shuttle.Hopper supports processing from both queues and stream transports.</p><p>All processing is performed on messages (serialized objects) that are received from a transport and by finding a message handler that can handle the type of the message (<code>instance.GetType().Name</code>). Messages are sent to a transport to be processed and this combination of transport and the <code>IBus</code> instance that performs the processing is referred to as an <em>endpoint</em>:</p><p><img src="'+n+'" alt="Endpoint Image"></p><p>It is important to note that <em>not</em> every <code>IBus</code> instance will necessarily process messages from an inbox. This happens when the instance is a producer of messages only. An example may be a web-base API that receives integration requests that are then sent to a relevant endpoint transport as a <code>command</code> message.</p><p>Similarly, not every transport is going to be consumed by a <code>IBus</code> instance. An example of this is the error queue where poison messages are routed to. These queues have to be managed out-of-band to determine the cause of the failure before moving the messages back to the inbox queue for another round of processing.</p><h2 id="stream-vs-queue-exception-handling" tabindex="-1">Stream vs Queue Exception Handling <a class="header-anchor" href="#stream-vs-queue-exception-handling" aria-label="Permalink to &quot;Stream vs Queue Exception Handling&quot;">​</a></h2><p>Streams are handled slightly differently to queues when it comes to exception handling.</p><p>When processing messages successfully from a <strong>queue</strong> a message is retrieved, handled, and then acknowledged. Once acknowledge, a message is removed from the queue. When a message fails it is <strong>re-queued</strong> for re-processing later on. It may also be moved to a deferred queue, if configured, before being moved back to the inbox work queue later on. The reason it is re-queued is that simply releasing the message would depend on the queuing mechanism to decide what to do with the message and it may remain at the head of the queue resulting in blocking any further movement on the consumer.</p><p>In contrast to queues, stream consumers do not ever remove messages from the stream. Streaming is a read-only operation for consumers. A stream is a log of messages that typically have a retention policy based on message age or size, or a combination thereof. As long as the messages are available in a stream any consumer may start reading from the stream at any point in the stream, although the start/oldest/tail of the stream or the end/newest/head of the stream would be typical starting positions. Each consumer has an offset which indicates where they are in the stream and once a message is acknowledged this offest is moved ahead to indicate the new position. It is up to the consuming application to decide how to handle exceptions. A message cannot be re-queued as that would result in a duplicate that would be picked up by other consumers are well. If a poison message is not dealt with the consumer will remain blocked. One way to deal with a poison message is to make use of an error (poison) queue. There is no straghtforward answer to dealing with these errors as it will depend on the type of error and use-case. There are also some <a href="/shuttle-hopper/guide/essentials/exception-handling.html">exception handling</a> indicators available on the <code>HandlerContext</code>.</p><h2 id="threading-number-of-consumers" tabindex="-1">Threading / Number of consumers <a class="header-anchor" href="#threading-number-of-consumers" aria-label="Permalink to &quot;Threading / Number of consumers&quot;">​</a></h2><p>For queues there is no real restriction on how many consumers one can create per queue. Each thread would result in a consumer. This is not the case for streams. Since a consumer moves forward along the stream/topic it does not make sense for another thread on the same consumer to be reading messages related to the same stream/topic from another location. In this case the service bus endpoint is one logical unit with, say, two threads. In order to perform parallel processing one would need the stream to be partitioned. This results in different logical/physical streams that may be processed independently. In this case the number of consumers may be equal to, or less than, the number of partitions. Having more consumers than partitions would result in the extra consumers remaining idle as they will never receive any messages.</p>',12)])])}const g=t(r,[["render",i]]);export{p as __pageData,g as default};
